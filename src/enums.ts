export enum Commands {
    Status = "status",
    Help = "help",
    WeedBad = "weedbad",
}

export enum ActivityTypes {
    PLAYING = "PLAYING",
    STREAMING = "STREAMING",
    LISTENING = "LISTENING",
    WATCHING = "WATCHING",
    COMPETING = "COMPETING"
}